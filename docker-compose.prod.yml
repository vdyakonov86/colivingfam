services:
  vpn_bot:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: colivingfam-vpn-bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/data